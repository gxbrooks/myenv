#!/bin/bash
#
# ⚠️  WARNING: This file is no longer needed!
# ⚠️  If your ~/.bashrc sources this file, please remove those lines.
#
# This .bashrc was originally designed to auto-run wake_on_kvm.sh,
# but that approach is problematic because wake_on_kvm.sh requires sudo.
#
# Instead, wake_on_kvm.sh is now run directly by setup_xfce4.sh.
#
# To clean up:
#   1. Edit your ~/.bashrc
#   2. Remove any lines that reference this file
#   3. Optionally delete this file (kept for reference)
#
